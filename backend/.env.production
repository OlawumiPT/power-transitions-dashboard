# Production environment variables
# NOTE: These are reference values. Actual production values must be set in Azure Portal.

NODE_ENV=production
DB_SCHEMA=pipeline_dashboard
FRONTEND_URL=https://platform.power-transitions.com
BACKEND_URL=https://pt-power-pipeline-api.azurewebsites.net

# These MUST be set in Azure Portal > Web App > Configuration > Application settings:
# - DATABASE_URL (full Postgres connection string)
# - JWT_SECRET (secure random value)
# - ADMIN_EMAIL (optional)
# - DEBUG_MODE=false
